services:
  - type: web
    name: medigen
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn medigen_main.wsgi:application"
    envVars:
      - fromGroup: medigen-env
